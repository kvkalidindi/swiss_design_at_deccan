# OS
Thumbs.db
.DS_Store
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# Build artifacts (placeholder — populated as the implementation progresses)
node_modules/
dist/
build/

# Local-only working files
*.local
*.tmp

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/

# Auto-generated intermediate data (re-derivable from scripts)
data/candidates.json
data/palette.json

# Keep tracked: logo, manual curation, vendored references
!data/logo.png
!data/anchors.json
!data/pantone-solid-coated.csv
!data/skill-transformations.json

# Temp clones
.tmp_swiss/

# Office lock files (Word/Excel/PowerPoint create these when a doc is open)
~$*

# Local-only template previews (PDFs rendered for visual verification)
outputs/template_previews/

# Deploy run logs (generated locally)
deploy/_elevated_run.log
